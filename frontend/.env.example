# ================================
# KabaadiSaathi Frontend — Vite Environment Variables
# ================================
# Copy this file to .env (inside the frontend/ folder) for local dev.
# In production/Docker, these are injected via build args.

# Base URL for all API calls.
# For local dev: Vite proxy rewrites /api → http://localhost:8080, so keep /api.
# For standalone deployment (backend on separate host):
#   VITE_API_URL=https://your-backend-host.com/api
VITE_API_URL=/api

# (Dev only) Override the Vite proxy target if your backend runs on a different port.
# Default: http://localhost:8080
# VITE_PROXY_TARGET=http://localhost:8080
